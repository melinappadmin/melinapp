"use client";import { FormEvent,useState } from "react";
export default function Login({success}:{success:()=>void}){const [error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget),r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});const j=await r.json();setLoading(false);if(!r.ok)return setError(j.error);success()}return <main className="login-page"><section className="login-brand"><img src="/logo.jpeg" alt="Paieiro Melin"/><span>GESTÃO INTELIGENTE</span><h1>Melin APP</h1><p>Estoque, clientes e reposições em um só lugar.</p></section><form className="login-card" onSubmit={submit}><span>ACESSO RESTRITO</span><h2>Entrar no painel</h2><p>Use suas credenciais administrativas.</p><label>E-mail<input name="email" type="email" autoComplete="username" required/></label><label>Senha<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<div className="login-error">{error}</div>}<button className="primary" disabled={loading}>{loading?"Entrando…":"Entrar"}</button></form></main>}
